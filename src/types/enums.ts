export enum bDealAction { Start, Abort, Delivered, Complain }

export enum sDealAction { Confirm, ChangeRendezvous, CallOff, Remove }

export enum DealState { Created, Agreed, Complete, Aborted, Archived, Removed }

export enum Reputation { BadActor, None, OneDeal, FewDeals, Established }
